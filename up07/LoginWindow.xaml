<Window x:Class="up07.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:up07"
        mc:Ignorable="d"
        Title="Вход в систему больницы" Height="450" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Background="#f5f5f5">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Заголовок-->
        <Border Grid.Row="0" Background="#2196f3">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Больница"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Система управления"
                           FontSize="14"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0 5 0 0"/>
            </StackPanel>
        </Border>

        <!--Форма входа-->
        <Border Grid.Row="1"
                CornerRadius="10"
                Margin="30 20 30 30">
            <StackPanel>
                <TextBlock Text="Вход в систему"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#333"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 20"/>
                <!--Логин-->
                <TextBlock Text="Логин:"
                           FontSize="14"
                           Foreground="#555"
                           Margin="0 0 0 5"/>
                <TextBox x:Name="LoginTextBox"
                         Height="35"
                         FontSize="14"
                         Padding="10 8"
                         BorderBrush="#bdbdbd"
                         BorderThickness="1"
                         Margin="0 0 0 15"/>

                <!--Пароль-->
                <TextBlock Text="Пароль:"
                           FontSize="14"
                           Foreground="#555"
                           Margin="0 0 0 5"/>
                <PasswordBox x:Name="PasswordBox"
                             Height="35"
                             FontSize="14"
                             Padding="10 8"
                             BorderBrush="#bdbdbd"
                             BorderThickness="1"
                             Margin="0 0 0 20"/>

                <!--Кнопка входа-->
                <Button Content="Войти"
                        Height="40"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Background="#2196f3"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="LoginButton_Click"
                        Margin="0 0 0 10">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="5"
                                                Padding="5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!--ссылка на регистрацию-->
                <TextBlock HorizontalAlignment="Center" Margin="0 10 0 0">
                    <Hyperlink Click="RegisterLink_Click"
                               TextDecorations="None"
                               Foreground="#2196f3">
                        <TextBlock Text="Нет аккаунта? Зарегистрироваться" FontSize="13"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
    <Window.Resources>
        <DropShadowEffect x:Key="DropShadow"
                          Color="#000000"
                          Direction="270"
                          ShadowDepth="2"
                          BlurRadius="10"
                          Opacity="0.15"/>
    </Window.Resources>
</Window>
